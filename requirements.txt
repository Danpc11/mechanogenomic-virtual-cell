numpy>=1.24
scipy>=1.10
numba>=0.57         # optional but strongly recommended (50-100x speed-up)
scikit-learn>=1.3   # calibration.py (GMM deconvolution)
pandas>=2.0         # calibration.py (CSV I/O)
gplearn>=0.4        # symbolic.py (symbolic regression)
sbi>=0.22 + torch   # enables neural posterior in inference.py
